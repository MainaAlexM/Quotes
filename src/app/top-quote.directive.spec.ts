import { TopQuoteDirective } from './top-quote.directive';

describe('TopQuoteDirective', () => {
  it('should create an instance', () => {
    const directive = new TopQuoteDirective();
    expect(directive).toBeTruthy();
  });
});
